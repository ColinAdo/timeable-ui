export type RowData = {
    id: string
    unit_code: string
    unit_name: string
    day: string
    time: string
    lecturer: string
    campus: string
    mode: string
    room: string
    group: string
}